// Top secret config file :)
// It visible for demo purposes only.

let serv = 'https://lighting-control-app.herokuapp.com:3000';

// if (process.env.NODE_ENV === 'development') {
//   serv = 'http://localhost:3001';
// }

module.exports = {
  serv: serv,
};
